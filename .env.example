# Copy to .env and fill values
PORT=8080
ADMIN_TOKEN=change-me-admin-token

# Postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ratelimiter?schema=public

# Redis
REDIS_URL=redis://localhost:6379/0

# Defaults for newly created keys (can be overridden per key or endpoint)
DEFAULT_RATE_PER_MINUTE=1000
DEFAULT_DAILY_QUOTA=100000
DEFAULT_BURST=200
